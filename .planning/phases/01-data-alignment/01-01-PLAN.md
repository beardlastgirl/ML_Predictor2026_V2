---
phase: 01-data-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/stats_engine.py, src/data_processing.py]
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Shin Method correctly removes overround from Pinnacle odds (PSCH/D/A)"
    - "Implied probabilities and market moves are added as features to the dataset"
  artifacts:
    - path: "src/stats_engine.py"
      provides: "shin_method implementation"
    - path: "src/data_processing.py"
      provides: "Odds feature engineering (implied probs, shifts)"
  key_links:
    - from: "src/data_processing.py"
      to: "src/stats_engine.py"
      via: "import and call shin_method"
---

<objective>
Implement the Shin Method for overround removal and integrate odds-based features into the data processing pipeline.

Purpose: To provide a fair market baseline and capture market sentiment as features without look-ahead leakage.
Output: Enhanced src/stats_engine.py and src/data_processing.py with odds-aware logic.
</objective>

<execution_context>
@C:/Scripts/ML_Predictor2026_V2/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Scripts/ML_Predictor2026_V2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/research/1-RESEARCH.md
@src/stats_engine.py
@src/data_processing.py

<interfaces>
From src/stats_engine.py:
```python
def calculate_team_stats(df):
    # Existing stat calculation logic
```

From src/data_processing.py:
```python
def preprocess_data(df):
    # Existing preprocessing logic
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Shin Method in stats_engine.py</name>
  <files>src/stats_engine.py</files>
  <behavior>
    - Given Pinnacle odds [2.0, 3.5, 4.0], return fair probabilities that sum to 1.0 using Shin's approach.
    - Handle edge cases like extremely high odds or missing values (NaN).
  </behavior>
  <action>
    Add the `shin_method` function to `src/stats_engine.py` as researched in 1-RESEARCH.md. 
    Use `scipy.optimize.brentq` for the root-finding part of the algorithm.
    The function should take a list/array of 3 odds and return (true_probs, z_optimal).
  </action>
  <verify>
    <automated>python -c "from src.stats_engine import shin_method; probs, z = shin_method([2.0, 3.5, 4.0]); print(f'Sum: {sum(probs)}'); assert abs(sum(probs) - 1.0) < 1e-6"</automated>
  </verify>
  <done>Shin Method implemented and verified with unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Update data_processing.py with Odds Features</name>
  <files>src/data_processing.py</files>
  <action>
    1. Update `preprocess_data` or add a specific function to handle odds engineering.
    2. Convert PSCH, PSCD, PSCA to fair probabilities using `shin_method` from `src.stats_engine`.
    3. Calculate Implied Probability Shift (Market Move): `1/Closing_Odds - 1/Opening_Odds` for Home, Draw, Away.
    4. Ensure these features are added to the dataframe and handles potential NaNs in odds data.
    5. Ensure zero leakage: Opening odds are known before the match, closing odds are known just before kickoff.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; from src.data_processing import preprocess_data; df = pd.read_csv('data/ARG.csv').head(10); df_proc = preprocess_data(df); assert 'PSH_fair' in df_proc.columns or 'PSCH_prob_shift' in df_proc.columns"</automated>
  </verify>
  <done>Data processing pipeline enriched with market-aware features.</done>
</task>

</tasks>

<verification>
Run a small test script that loads ARG.csv, processes it, and prints the new odds-based columns to verify alignment and values.
</verification>

<success_criteria>
- Shin Method passes accuracy tests (sum to 1.0).
- Dataframe contains new features: fair probabilities (Shin-normalized) and market move shifts.
- No look-ahead leakage in the newly created features.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-alignment/01-01-SUMMARY.md`
</output>
