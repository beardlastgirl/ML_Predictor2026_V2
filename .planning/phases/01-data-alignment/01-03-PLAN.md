---
phase: 01-data-alignment
plan: 03
type: execute
wave: 3
depends_on: ["01-01"]
files_modified: [src/data_processing.py, src/features.py]
autonomous: true
requirements: [DATA-04]

must_haves:
  truths:
    - "External xG data (FBref/FootyStats) is successfully merged with the main ARG.csv dataset"
    - "Fuzzy matching correctly identifies team names when exact matches fail"
    - "Relative xG (Strength vs Average) is added as a feature"
  artifacts:
    - path: "src/data_processing.py"
      provides: "Merging utility with fuzzy matching logic"
    - path: "src/features.py"
      provides: "Relative xG and Strength vs Average feature engineering"
  key_links:
    - from: "src/data_processing.py"
      to: "data/ARG.csv"
      via: "read and join operations"
---

<objective>
Implement a robust data merging utility for xG data and integrate advanced xG-based features into the pipeline.

Purpose: To enrich the dataset with expected goals (xG) metrics to capture underlying team performance better than actual goals.
Output: Merged dataset and new features in src/features.py.
</objective>

<execution_context>
@C:/Scripts/ML_Predictor2026_V2/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Scripts/ML_Predictor2026_V2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/1-CONTEXT.md
@.planning/research/1-RESEARCH.md
@src/data_processing.py
@src/features.py
@data/ARG.csv

<interfaces>
From src/data_processing.py:
```python
def merge_external_data(df, external_csv):
    # logic for joining
```

From src/features.py:
```python
def add_xg_features(df):
    # logic for calculating relative xG
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement xG Data Merging in data_processing.py</name>
  <files>src/data_processing.py</files>
  <action>
    1. Create a function `merge_xg_data(df, xg_files_list)` to hard-merge FBref/FootyStats CSVs with the main ARG dataframe.
    2. Use `Glossary.txt` for primary team-name mapping.
    3. Implement a fuzzy matching fallback using `rapidfuzz` (threshold 85%) for unmapped names.
    4. Primary merge keys: Date, HomeTeam (Normalized), AwayTeam (Normalized).
    5. Backfill older seasons (pre-xG) with actual GF/GA to maintain volume.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; from src.data_processing import merge_xg_data; df = pd.read_csv('data/ARG.csv').head(10); # (need xG files locally for full test)"</automated>
  </verify>
  <done>xG data merged into the main dataset with fuzzy matching fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Engineer Advanced xG Features in features.py</name>
  <files>src/features.py</files>
  <action>
    Implement new feature calculations based on merged xG data:
    1. **Relative xG:** Calculate `Team_xG / League_Season_Avg_xG` for both home and away teams.
    2. **Strength vs Average:** Normalize xG metrics per season to account for league-wide changes.
    3. **Rolling xG:** Update form metrics to include average xG over the last 5/10 matches.
    Ensure features handle NaNs gracefully (especially for seasons without xG).
  </action>
  <verify>
    <automated>python -c "import pandas as pd; from src.features import add_xg_features; df = pd.DataFrame({'Home_xG': [1.2, 1.5], 'Season': [2024, 2024]}); df_f = add_xg_features(df); assert 'Relative_xG' in df_f.columns"</automated>
  </verify>
  <done>xG-based features integrated into the feature engineering module.</done>
</task>

</tasks>

<verification>
Process the full dataset and verify that the output CSV contains non-null xG features for recent seasons and fallback values for older ones.
Check that `Relative_xG` values cluster around 1.0.
</verification>

<success_criteria>
- Merge rate for xG data is high (>95% for recent seasons).
- Feature set includes relative and rolling xG metrics.
- Pipeline continues to train successfully without leakage errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-alignment/01-03-SUMMARY.md`
</output>
