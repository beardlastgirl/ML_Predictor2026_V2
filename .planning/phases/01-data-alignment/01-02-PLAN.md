---
phase: 01-data-alignment
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/model_engine.py, src/pipeline.py]
autonomous: true
requirements: [MODEL-01, MODEL-03]

must_haves:
  truths:
    - "System calculates Naive (freq-based) and Bookmaker (fair-prob) baselines for each test fold"
    - "Validation loop skips 2012-2014 matches for evaluation, only using them for training/history"
    - "Metrics are reported season-by-season and broken down by Home/Draw/Away outcomes"
  artifacts:
    - path: "src/model_engine.py"
      provides: "Baseline models (Naive, Historical, Bookmaker)"
    - path: "src/pipeline.py"
      provides: "Updated CV loop with season tracking and metric breakdowns"
  key_links:
    - from: "src/pipeline.py"
      to: "src/model_engine.py"
      via: "instantiate and evaluate baseline models alongside primary model"
---

<objective>
Implement benchmark baseline models and update the validation pipeline for season-by-season evaluation with Log Loss metrics.

Purpose: To establish a "ground truth" performance floor and monitor model calibration using probabilistic metrics.
Output: Integrated baselines in src/model_engine.py and refined validation in src/pipeline.py.
</objective>

<execution_context>
@C:/Scripts/ML_Predictor2026_V2/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Scripts/ML_Predictor2026_V2/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/1-CONTEXT.md
@.planning/research/1-RESEARCH.md
@src/model_engine.py
@src/pipeline.py

<interfaces>
From src/model_engine.py:
```python
class ModelEngine:
    def predict_proba(self, X):
        # returns probabilities
```

From src/pipeline.py:
```python
def run_pipeline(config):
    # Cross-validation and evaluation logic
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Baseline models in model_engine.py</name>
  <files>src/model_engine.py</files>
  <action>
    Create a new class or update `ModelEngine` to support baseline predictions:
    1. **Naive Baseline:** Return league-wide historical probabilities (e.g., [0.4, 0.35, 0.25]) for all matches.
    2. **Historical Baseline:** Return probabilities based on the previous season's actual frequencies for each team or league.
    3. **Bookmaker Baseline:** Return fair probabilities (Shin-normalized) directly from Pinnacle odds.
    Ensure `predict_proba` returns (Home, Draw, Away) probabilities for these baselines.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; from src.model_engine import NaiveBaseline; baseline = NaiveBaseline(); probs = baseline.predict_proba(pd.DataFrame({'a':[1]})); print(probs); assert abs(sum(probs[0]) - 1.0) < 1e-6"</automated>
  </verify>
  <done>Baseline models implemented and capable of producing 1X2 probabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Validation Pipeline in pipeline.py</name>
  <files>src/pipeline.py</files>
  <action>
    Update the cross-validation loop in `src/pipeline.py`:
    1. Implement a **Season-by-Season** reporting loop.
    2. Filter evaluation matches: Skip the "warm-up" period (2012-08-01 to 2014-07-31) in final metrics, though they remain in training sets.
    3. Add **Home/Draw/Away Log Loss** calculations for the primary model and all baselines.
    4. Store results in a structure that allows printing a table of metrics per season.
    5. Maintain the 0.7 draw weighting for internal model training (if applicable) but ensure evaluation is fair.
  </action>
  <verify>
    <automated>python src/pipeline.py --test-mode --skip-plots</automated>
  </verify>
  <done>Validation loop updated with rigorous benchmarking and detailed metric reporting.</done>
</task>

</tasks>

<verification>
Execute the updated pipeline and verify that the output summary includes:
1. Log Loss for primary model vs Baselines.
2. Season-by-season breakdowns.
3. Home/Draw/Away specific performance metrics.
</verification>

<success_criteria>
- Validation report shows Log Loss for all 3 baselines (Naive, Historical, Bookmaker).
- Results for 2012-2014 are excluded from final aggregated metrics.
- Output includes seasonal performance evolution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-alignment/01-02-SUMMARY.md`
</output>
